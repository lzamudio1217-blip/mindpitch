# MindPitch Environment Variables
# Copy this file to .env locally, or add the same variable in Vercel.

# Server-side Anthropic API key. Do NOT prefix with REACT_APP_ because the key should not be exposed in the browser bundle.
ANTHROPIC_API_KEY=sk-ant-your-key-here
